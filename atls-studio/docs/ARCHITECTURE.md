# Architecture

The canonical technical architecture document is at the **repository root**:

**[ARCHITECTURE.md](../../../ARCHITECTURE.md)**

From this file (`atls-studio/atls-studio/docs/`), `../../../` reaches the clone root. Do not maintain a second full copy here — update the root file only.
